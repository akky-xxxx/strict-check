const DIRECTORIES = {
  WS_MODULES: "modules",
  WS_PACKAGES: "packages",

  ESLINT_CONFIG: "eslint-config",
  ESLINT_PLUGIN: "eslint-plugin",

  DEST_CONFIG: "shared-for-config",
  DEST_PLUGIN: "shared-for-plugin",

  CONFIG: "config",
  LIBS: "libs",
  SHARED_PREFIX: "shared",
  SRC: "src",
}

module.exports = {
  DIRECTORIES,
}
