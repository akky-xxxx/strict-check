export const FILES = {
  BASE: [
    "**/*{spec,specs,SPEC,SPECS,test,tests,TEST,TESTS}*/**",
    "**/*.{spec,test}.*",
  ],
  JS: [
    "**/{spec,specs,SPEC,SPECS,test,tests,TEST,TESTS}/**/*.js{,x}",
    "**/*.{spec,test}.js{,x}",
  ],
  TS: [
    "**/{spec,specs,SPEC,SPECS,test,tests,TEST,TESTS}/**/*.ts{,x}",
    "**/*.{spec,test}.ts{,x}",
  ],
}
