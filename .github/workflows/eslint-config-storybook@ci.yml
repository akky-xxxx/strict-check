name: eslint-config-storybook@CI

permissions:
  contents: read

on:
  pull_request:
    branches:
      - develop
      - feature/**
      - main
    paths:
      - '.node-version'
      - 'config/**'
      - 'modules/shared-for-eslint-config/**'
      - 'packages/eslint-config-storybook/**'
      - 'prettier.config.js'
      - 'yarn.lock'

jobs:
  check-code:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [ 18.x, 20.x ]
    defaults:
      run:
        working-directory: ./packages/eslint-config-storybook
    steps:
      - uses: actions/checkout@v4

      - name: setup
        uses: ./.github/actions/ci-setup

      - name: Run prepare
        run: yarn prepare

      - name: Run lint
        run: yarn lint

      - name: Run spell-check
        run: yarn spell-check

      - name: Run type-check
        run: yarn type-check

      - name: Run dry run
        run: yarn dry-run
