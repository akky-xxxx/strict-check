name: local-eslint-config-storybook@CI

permissions:
  contents: read

on:
  pull_request:
    branches:
      - local/**
    paths:
      - '.node-version'
      - 'config/**'
      - 'dry-run/dr-eslint-config-sc-storybook/**'
      - 'modules/shared-for-eslint-config/**'
      - 'packages/eslint-config-storybook/**'
      - 'prettier.config.js'
      - 'yarn.lock'

env:
  WORKING_DIRECTORY: ./packages/eslint-config-storybook

defaults:
  run:
    working-directory: ${{ env.WORKING_DIRECTORY }}

jobs:
  local-dry-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: setup
        uses: ./.github/actions/common-setup

      - name: Run prepare
        run: yarn prepare

      - name: Run lint
        run: yarn lint


      - name: Run spell-check
        run: yarn spell-check

      - name: Run type-check
        run: yarn type-check


      - name: Run dry run
        run: yarn dry-run
